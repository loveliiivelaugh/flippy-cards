import React from 'react'
import { IconButton } from '@material-ui/core';


const Header = () => {
  return (
    <div>
      <IconButton onClick={() => {} }>
        Menu
      </IconButton>
      <IconButton onClick={() => {} }>
        Speak
      </IconButton>
      <IconButton onClick={() => {} }>
        Type
      </IconButton>
    </div>
  )
}

export default Header
